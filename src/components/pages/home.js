// @flow

import React from 'react';

import type { Element } from 'react';

import { Page } from '../../components/elements/page';

const Home = (): Element<*> => (
    <Page header="Home">
        <div>Home dupa</div>
    </Page>
);

export default Home;
